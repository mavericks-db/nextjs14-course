export default function Menu(props: any) {
    return (
        <>
            <h1>Menu</h1>
            {props.name}
            {props.age}
            {props.isMan}
            {props.children}
       </> 
    )
} 
        
        